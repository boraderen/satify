import { GraphProblemWorkbench } from "@/components/GraphProblemWorkbench";

export default function IndependentSetPage() {
  return (
    <main className="page-shell problem-page">
      <GraphProblemWorkbench mode="independent-set" />
    </main>
  );
}
